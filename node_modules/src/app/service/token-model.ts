export interface TokenModel {
  // username: string,
  // title: string,
  // first_name: string,
  // last_name: string,
  // email: string,
  tokens: {
    access: string;
    refresh: string;
  }
}
